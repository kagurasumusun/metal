// -*- Metal -*-
//===-- __bits/metal_texturecube ------------------------------------------===//
// Copyright (c) 2014-2024 Apple Inc. All rights reserved
//===----------------------------------------------------------------------===//

#ifndef __BITS_METAL_TEXTURECUBE
#define __BITS_METAL_TEXTURECUBE

#include <__bits/metal_texture_common>

#if defined(__HAVE_ARRAY__)
#include <metal_array>
#endif
#if defined(__HAVE_IMAGEBLOCKS__)
#include <metal_imageblocks>
#endif

#pragma METAL internals : enable
namespace metal
{
template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_sample
{
};

#if defined(__HAVE_TEXTURE_ULONG_COLOR_CHANNEL__)
template <access a, __memory_coherence c>
struct _texturecube_sample<ulong, a, c, typename enable_if<_is_valid_sample(a, c)>::type>
{
};
#endif

template <typename T, access a, __memory_coherence c>
struct _texturecube_sample<T, a, c, typename enable_if<_is_valid_sample(a, c)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias options) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, level options) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, min_lod_clamp options) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube options) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias options) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, level options) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, min_lod_clamp options) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube options) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_CALCULATE_LOD__)
  METAL_FUNC float calculate_clamped_lod(sampler s, float3 coord) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_calculate_clamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
  METAL_FUNC float calculate_unclamped_lod(sampler s, float3 coord) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_calculate_unclamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias options) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, level options) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, min_lod_clamp options) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube options) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias options) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, level options) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, min_lod_clamp options) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube options) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_CALCULATE_LOD__)
  METAL_FUNC float calculate_clamped_lod(sampler s, float3 coord) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_calculate_clamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
  METAL_FUNC float calculate_unclamped_lod(sampler s, float3 coord) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_calculate_unclamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias options) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, level options) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, min_lod_clamp options) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube options) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias options) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, level options) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, min_lod_clamp options) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube options) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_CALCULATE_LOD__)
  METAL_FUNC float calculate_clamped_lod(sampler s, float3 coord) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_calculate_clamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
  METAL_FUNC float calculate_unclamped_lod(sampler s, float3 coord) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_calculate_unclamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias options) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, level options) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, min_lod_clamp options) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube options) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias options) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, level options) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, min_lod_clamp options) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube options) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_CALCULATE_LOD__)
  METAL_FUNC float calculate_clamped_lod(sampler s, float3 coord) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_calculate_clamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
  METAL_FUNC float calculate_unclamped_lod(sampler s, float3 coord) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_calculate_unclamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias options) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, level options) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, min_lod_clamp options) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube options) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias options) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, level options) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, min_lod_clamp options) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube options) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_CALCULATE_LOD__)
  METAL_FUNC float calculate_clamped_lod(sampler s, float3 coord) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_calculate_clamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
  METAL_FUNC float calculate_unclamped_lod(sampler s, float3 coord) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_calculate_unclamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
#endif
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias options) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, level options) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, min_lod_clamp options) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube options) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC vec<T, 4> sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#endif
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias options) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, options.value, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, level options) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, true, options.lod, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, min_lod_clamp options) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, 0.0f, options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, bias bias_options, min_lod_clamp min_lod_clamp_options) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t(derived.t, s.val, coord, false, bias_options.value, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube options) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, options.dPdx, options.dPdy, 0.0f, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#if defined(__HAVE_MIN_LOD_CLAMP__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_sample(sampler s, float3 coord, gradientcube grad_options, min_lod_clamp min_lod_clamp_options) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_sample_texture_cube_t_grad(derived.t, s.val, coord, grad_options.dPdx, grad_options.dPdy, min_lod_clamp_options.value, int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_CALCULATE_LOD__)
  METAL_FUNC float calculate_clamped_lod(sampler s, float3 coord) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_calculate_clamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
  METAL_FUNC float calculate_unclamped_lod(sampler s, float3 coord) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_calculate_unclamped_lod_texture_cube_t(derived.t, s.val, coord, int(a));
  }
#endif
#endif
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_read
{
};

#if defined(__HAVE_TEXTURE_CUBE_READ__)
#if defined(__HAVE_TEXTURE_ULONG_COLOR_CHANNEL__) && !defined(__HAVE_TEXTURE_CUBE_ULONG_COLOR_CHANNEL__)
template <access a, __memory_coherence c>
struct _texturecube_read<ulong, a, c, typename enable_if<_is_valid_read(a, c)>::type>
{
};
#endif

template <typename T, access a, __memory_coherence c>
struct _texturecube_read<T, a, c, typename enable_if<_is_valid_read(a, c)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC vec<T, 4> read(ushort2 coord, ushort face, ushort lod = 0) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(ushort2 coord, ushort face, ushort lod = 0) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
  METAL_FUNC vec<T, 4> read(uint2 coord, uint face, uint lod = 0) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(uint2 coord, uint face, uint lod = 0) const thread 
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC vec<T, 4> read(ushort2 coord, ushort face, ushort lod = 0) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(ushort2 coord, ushort face, ushort lod = 0) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
  METAL_FUNC vec<T, 4> read(uint2 coord, uint face, uint lod = 0) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(uint2 coord, uint face, uint lod = 0) const device 
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC vec<T, 4> read(ushort2 coord, ushort face, ushort lod = 0) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(ushort2 coord, ushort face, ushort lod = 0) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
  METAL_FUNC vec<T, 4> read(uint2 coord, uint face, uint lod = 0) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(uint2 coord, uint face, uint lod = 0) const device coherent(device)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC vec<T, 4> read(ushort2 coord, ushort face, ushort lod = 0) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(ushort2 coord, ushort face, ushort lod = 0) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
  METAL_FUNC vec<T, 4> read(uint2 coord, uint face, uint lod = 0) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(uint2 coord, uint face, uint lod = 0) const constant 
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC vec<T, 4> read(ushort2 coord, ushort face, ushort lod = 0) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(ushort2 coord, ushort face, ushort lod = 0) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
  METAL_FUNC vec<T, 4> read(uint2 coord, uint face, uint lod = 0) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(uint2 coord, uint face, uint lod = 0) const ray_data 
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC vec<T, 4> read(ushort2 coord, ushort face, ushort lod = 0) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(ushort2 coord, ushort face, ushort lod = 0) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
  METAL_FUNC vec<T, 4> read(uint2 coord, uint face, uint lod = 0) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_read(uint2 coord, uint face, uint lod = 0) const object_data 
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_read_texture_cube_t(derived.t, __metal_get_read_sampler(), coord, face, lod, int(a), int(c), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
};
#endif

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_write
{
};

#if defined(__HAVE_TEXTURE_CUBE_WRITE__)
#if defined(__HAVE_TEXTURE_ULONG_COLOR_CHANNEL__) && !defined(__HAVE_TEXTURE_CUBE_ULONG_COLOR_CHANNEL__)
template <access a, __memory_coherence c>
struct _texturecube_write<ulong, a, c, typename enable_if<_is_valid_write(a, c)>::type>
{
};
#endif

template <typename T, access a, __memory_coherence c>
struct _texturecube_write<T, a, c, typename enable_if<_is_valid_write(a, c)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC void write(vec<T, 4> color, ushort2 coord, ushort face, ushort lod = 0) const thread  METAL_VALID_LOD_ARG(lod)
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, ushort2 coord, ushort face, ushort lod = 0) const thread  METAL_VALID_LOD_ARG(lod)
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
  METAL_FUNC void write(vec<T, 4> color, uint2 coord, uint face, uint lod = 0) const thread  METAL_VALID_LOD_ARG(lod)
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, uint2 coord, uint face, uint lod = 0) const thread  METAL_VALID_LOD_ARG(lod)
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC void write(vec<T, 4> color, ushort2 coord, ushort face, ushort lod = 0) const device  METAL_VALID_LOD_ARG(lod)
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, ushort2 coord, ushort face, ushort lod = 0) const device  METAL_VALID_LOD_ARG(lod)
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
  METAL_FUNC void write(vec<T, 4> color, uint2 coord, uint face, uint lod = 0) const device  METAL_VALID_LOD_ARG(lod)
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, uint2 coord, uint face, uint lod = 0) const device  METAL_VALID_LOD_ARG(lod)
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC void write(vec<T, 4> color, ushort2 coord, ushort face, ushort lod = 0) const device coherent(device) METAL_VALID_LOD_ARG(lod)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, ushort2 coord, ushort face, ushort lod = 0) const device coherent(device) METAL_VALID_LOD_ARG(lod)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
  METAL_FUNC void write(vec<T, 4> color, uint2 coord, uint face, uint lod = 0) const device coherent(device) METAL_VALID_LOD_ARG(lod)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, uint2 coord, uint face, uint lod = 0) const device coherent(device) METAL_VALID_LOD_ARG(lod)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC void write(vec<T, 4> color, ushort2 coord, ushort face, ushort lod = 0) const constant  METAL_VALID_LOD_ARG(lod)
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, ushort2 coord, ushort face, ushort lod = 0) const constant  METAL_VALID_LOD_ARG(lod)
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
  METAL_FUNC void write(vec<T, 4> color, uint2 coord, uint face, uint lod = 0) const constant  METAL_VALID_LOD_ARG(lod)
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, uint2 coord, uint face, uint lod = 0) const constant  METAL_VALID_LOD_ARG(lod)
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC void write(vec<T, 4> color, ushort2 coord, ushort face, ushort lod = 0) const ray_data  METAL_VALID_LOD_ARG(lod)
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, ushort2 coord, ushort face, ushort lod = 0) const ray_data  METAL_VALID_LOD_ARG(lod)
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
  METAL_FUNC void write(vec<T, 4> color, uint2 coord, uint face, uint lod = 0) const ray_data  METAL_VALID_LOD_ARG(lod)
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, uint2 coord, uint face, uint lod = 0) const ray_data  METAL_VALID_LOD_ARG(lod)
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_16B_COORDS__)
  METAL_FUNC void write(vec<T, 4> color, ushort2 coord, ushort face, ushort lod = 0) const object_data  METAL_VALID_LOD_ARG(lod)
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, ushort2 coord, ushort face, ushort lod = 0) const object_data  METAL_VALID_LOD_ARG(lod)
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
  METAL_FUNC void write(vec<T, 4> color, uint2 coord, uint face, uint lod = 0) const object_data  METAL_VALID_LOD_ARG(lod)
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_write_texture_cube_t(derived.t, coord, face, color, lod, int(a), int(c), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#if defined(__HAVE_IMAGEBLOCKS__)
  template <typename E, typename L>
  METAL_FUNC typename enable_if<_is_valid_imageblock_slice_write(E, T, c)>::type write(imageblock_slice<E, L> slice, uint2 coord, uint face, uint lod = 0) const object_data  METAL_VALID_LOD_ARG(lod)
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_write_imageblock_slice_to_texture_cube_t(derived.t, slice._imgblkptr, slice._size_valid, slice._size, coord, face, lod, is_same_v<L, imageblock_layout_implicit>, int(a), __METAL_TEXTURE_WRITE_ROUNDING_MODE__);
  }
#endif
#endif
};
#endif

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_gather
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_gather<T, a, c, typename enable_if<_is_valid_gather(a, c)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC vec<T, 4> gather(sampler s, float3 coord, component cmpt = component::x) const thread  METAL_CONST_ARG(cmpt)
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_gather(sampler s, float3 coord, component cmpt = component::x) const thread  METAL_CONST_ARG(cmpt)
  {
    bool nack;
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    vec<T, 4> v = __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC vec<T, 4> gather(sampler s, float3 coord, component cmpt = component::x) const device  METAL_CONST_ARG(cmpt)
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_gather(sampler s, float3 coord, component cmpt = component::x) const device  METAL_CONST_ARG(cmpt)
  {
    bool nack;
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    vec<T, 4> v = __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC vec<T, 4> gather(sampler s, float3 coord, component cmpt = component::x) const device coherent(device) METAL_CONST_ARG(cmpt)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_gather(sampler s, float3 coord, component cmpt = component::x) const device coherent(device) METAL_CONST_ARG(cmpt)
  {
    bool nack;
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    vec<T, 4> v = __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC vec<T, 4> gather(sampler s, float3 coord, component cmpt = component::x) const constant  METAL_CONST_ARG(cmpt)
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_gather(sampler s, float3 coord, component cmpt = component::x) const constant  METAL_CONST_ARG(cmpt)
  {
    bool nack;
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    vec<T, 4> v = __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC vec<T, 4> gather(sampler s, float3 coord, component cmpt = component::x) const ray_data  METAL_CONST_ARG(cmpt)
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_gather(sampler s, float3 coord, component cmpt = component::x) const ray_data  METAL_CONST_ARG(cmpt)
  {
    bool nack;
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC vec<T, 4> gather(sampler s, float3 coord, component cmpt = component::x) const object_data  METAL_CONST_ARG(cmpt)
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), static_cast<thread bool*>(nullptr), vec<T, 4>());
  }
#if defined(__HAVE_SPARSE_TEXTURES__)
  METAL_FUNC sparse_color<vec<T, 4>> sparse_gather(sampler s, float3 coord, component cmpt = component::x) const object_data  METAL_CONST_ARG(cmpt)
  {
    bool nack;
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    vec<T, 4> v = __metal_gather_texture_cube_t(derived.t, s.val, coord, int(cmpt), int(a), &nack, vec<T, 4>());
    return {v, !nack};
  }
#endif
#endif
};

#if defined(__HAVE_TEXTURE_CUBE_RELAXED_ORDER_ATOMIC__)
template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_load
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_atomic_load<T, a, c, typename enable_if<_is_valid_read(a, c) && _is_color_texture_atomic_channel_type(T)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC vec<T, 4> atomic_load(ushort2 coord, ushort face) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(uint2 coord, uint face) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(ushort2 coord, ushort face) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(uint2 coord, uint face) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC vec<T, 4> atomic_load(ushort2 coord, ushort face) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(uint2 coord, uint face) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
#endif
  METAL_FUNC vec<T, 4> atomic_load(ushort2 coord, ushort face) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(uint2 coord, uint face) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(ushort2 coord, ushort face) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(uint2 coord, uint face) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(ushort2 coord, ushort face) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
  METAL_FUNC vec<T, 4> atomic_load(uint2 coord, uint face) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_load_explicit_texture_cube_t(derived.t, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a), vec<T, 4>());
  }
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_store
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_atomic_store<T, a, c, typename enable_if<_is_valid_write(a, c) && _is_color_texture_atomic_channel_type(T)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC void atomic_store(vec<T, 4> color, ushort2 coord, ushort face) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, uint2 coord, uint face) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, ushort2 coord, ushort face) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, uint2 coord, uint face) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC void atomic_store(vec<T, 4> color, ushort2 coord, ushort face) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, uint2 coord, uint face) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#endif
  METAL_FUNC void atomic_store(vec<T, 4> color, ushort2 coord, ushort face) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, uint2 coord, uint face) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, ushort2 coord, ushort face) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, uint2 coord, uint face) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, ushort2 coord, ushort face) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_store(vec<T, 4> color, uint2 coord, uint face) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_atomic_store_explicit_texture_cube_t(derived.t, color, coord, face, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_compare_exchange
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_atomic_compare_exchange<T, a, c, typename enable_if<_is_valid_read(a, c) && _is_valid_write(a, c) && _is_color_texture_atomic_channel_type(T)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC bool atomic_compare_exchange_weak(ushort2 coord, ushort face, thread vec<T, 4> *expected, vec<T, 4> desired) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(uint2 coord, uint face, thread vec<T, 4> *expected, vec<T, 4> desired) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(ushort2 coord, ushort face, thread vec<T, 4> *expected, vec<T, 4> desired) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(uint2 coord, uint face, thread vec<T, 4> *expected, vec<T, 4> desired) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC bool atomic_compare_exchange_weak(ushort2 coord, ushort face, thread vec<T, 4> *expected, vec<T, 4> desired) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(uint2 coord, uint face, thread vec<T, 4> *expected, vec<T, 4> desired) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#endif
  METAL_FUNC bool atomic_compare_exchange_weak(ushort2 coord, ushort face, thread vec<T, 4> *expected, vec<T, 4> desired) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(uint2 coord, uint face, thread vec<T, 4> *expected, vec<T, 4> desired) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(ushort2 coord, ushort face, thread vec<T, 4> *expected, vec<T, 4> desired) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(uint2 coord, uint face, thread vec<T, 4> *expected, vec<T, 4> desired) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(ushort2 coord, ushort face, thread vec<T, 4> *expected, vec<T, 4> desired) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC bool atomic_compare_exchange_weak(uint2 coord, uint face, thread vec<T, 4> *expected, vec<T, 4> desired) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_compare_exchange_weak_explicit_texture_cube_t(derived.t, coord, face, expected, desired, __METAL_MEMORY_ORDER_RELAXED__, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_exchange
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_atomic_exchange<T, a, c, typename enable_if<_is_valid_read(a, c) && _is_valid_write(a, c) && _is_color_texture_atomic_channel_type(T)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC vec<T, 4> atomic_exchange(ushort2 coord, ushort face, vec<T, 4> desired) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(uint2 coord, uint face, vec<T, 4> desired) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(ushort2 coord, ushort face, vec<T, 4> desired) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(uint2 coord, uint face, vec<T, 4> desired) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC vec<T, 4> atomic_exchange(ushort2 coord, ushort face, vec<T, 4> desired) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(uint2 coord, uint face, vec<T, 4> desired) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#endif
  METAL_FUNC vec<T, 4> atomic_exchange(ushort2 coord, ushort face, vec<T, 4> desired) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(uint2 coord, uint face, vec<T, 4> desired) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(ushort2 coord, ushort face, vec<T, 4> desired) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(uint2 coord, uint face, vec<T, 4> desired) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(ushort2 coord, ushort face, vec<T, 4> desired) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_exchange(uint2 coord, uint face, vec<T, 4> desired) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_exchange_explicit_texture_cube_t(derived.t, coord, face, desired, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_fetch_and
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_atomic_fetch_and<T, a, c, typename enable_if<_is_valid_read(a, c) && _is_valid_write(a, c) && _is_color_texture_atomic_channel_type(T)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC vec<T, 4> atomic_fetch_and(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC vec<T, 4> atomic_fetch_and(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#endif
  METAL_FUNC vec<T, 4> atomic_fetch_and(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_and(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_and_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_max(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_min(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_or(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_or_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_xor(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_xor_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_fetch_and_arith
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube_atomic_fetch_and_arith<T, a, c, typename enable_if<_is_valid_read(a, c) && _is_valid_write(a, c) && _is_color_texture_atomic_channel_type(T)>::type>
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
  METAL_FUNC vec<T, 4> atomic_fetch_add(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(ushort2 coord, ushort face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(uint2 coord, uint face, vec<T, 4> operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(ushort2 coord, ushort face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(uint2 coord, uint face, vec<T, 4> operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC vec<T, 4> atomic_fetch_add(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(ushort2 coord, ushort face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(uint2 coord, uint face, vec<T, 4> operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#endif
  METAL_FUNC vec<T, 4> atomic_fetch_add(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(ushort2 coord, ushort face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(uint2 coord, uint face, vec<T, 4> operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(ushort2 coord, ushort face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(uint2 coord, uint face, vec<T, 4> operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(ushort2 coord, ushort face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_add(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_add_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC vec<T, 4> atomic_fetch_sub(uint2 coord, uint face, vec<T, 4> operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_atomic_fetch_sub_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
};

template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube_atomic_modify
{
};

template <access a, __memory_coherence c>
struct _texturecube_atomic_modify<ulong, a, c, typename enable_if<_is_valid_read(a, c) && _is_valid_write(a, c)>::type>
{
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<ulong, a, c>;
#else
  using __derived_t = texturecube<ulong, a>;
#endif

public:
  METAL_FUNC void atomic_max(ushort2 coord, ushort face, ulong4 operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(ushort2 coord, ushort face, ulong4 operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(uint2 coord, uint face, ulong4 operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(uint2 coord, uint face, ulong4 operand) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(ushort2 coord, ushort face, ulong4 operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(ushort2 coord, ushort face, ulong4 operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(uint2 coord, uint face, ulong4 operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(uint2 coord, uint face, ulong4 operand) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#if defined(__HAVE_COHERENT__)
  METAL_FUNC void atomic_max(ushort2 coord, ushort face, ulong4 operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(ushort2 coord, ushort face, ulong4 operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(uint2 coord, uint face, ulong4 operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(uint2 coord, uint face, ulong4 operand) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
#endif
  METAL_FUNC void atomic_max(ushort2 coord, ushort face, ulong4 operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(ushort2 coord, ushort face, ulong4 operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(uint2 coord, uint face, ulong4 operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(uint2 coord, uint face, ulong4 operand) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(ushort2 coord, ushort face, ulong4 operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(ushort2 coord, ushort face, ulong4 operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(uint2 coord, uint face, ulong4 operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(uint2 coord, uint face, ulong4 operand) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(ushort2 coord, ushort face, ulong4 operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(ushort2 coord, ushort face, ulong4 operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_max(uint2 coord, uint face, ulong4 operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_atomic_max_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
  METAL_FUNC void atomic_min(uint2 coord, uint face, ulong4 operand) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    __metal_atomic_min_explicit_texture_cube_t(derived.t, coord, face, operand, __METAL_MEMORY_ORDER_RELAXED__, int(a));
  }
};
#endif


template <typename T, access a, __memory_coherence c, typename _Enable = void>
struct _texturecube
{
};

template <typename T, access a, __memory_coherence c>
struct _texturecube<T, a, c, typename enable_if<is_color_texture_channel<T>::value>::type>
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  : _deleted_operators<texturecube<T, a, c>>
  , _texture_explicit_conversion<c == memory_coherence_device, texturecube<T, a, c>, texturecube<T, a, memory_coherence_threadgroup>>
  , _texture_implicit_conversion<c == memory_coherence_threadgroup, texturecube<T, a, c>, texturecube<T, a, memory_coherence_device>>
#else
  : _deleted_operators<texturecube<T, a>>
#endif
  , _texturecube_sample<T, a, c>
  , _texturecube_read<T, a, c>
  , _texturecube_write<T, a, c>
  , _texturecube_gather<T, a, c>
#if defined(__HAVE_TEXTURE_CUBE_RELAXED_ORDER_ATOMIC__)
  , _texturecube_atomic_load<T, a, c>
  , _texturecube_atomic_store<T, a, c>
  , _texturecube_atomic_compare_exchange<T, a, c>
  , _texturecube_atomic_exchange<T, a, c>
  , _texturecube_atomic_fetch_and<T, a, c>
  , _texturecube_atomic_fetch_and_arith<T, a, c>
  , _texturecube_atomic_modify<T, a, c>
#endif
{
private:
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  using __derived_t = texturecube<T, a, c>;
#else
  using __derived_t = texturecube<T, a>;
#endif

public:
#if defined(__HAVE_TEXTURE_READWRITE__)
  METAL_FUNC void fence() thread 
  {
    const thread  auto &derived = static_cast<thread  __derived_t &>(*this);
    __metal_fence_texture_cube_t(derived.t);
  }
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC void fence() device 
  {
    const device  auto &derived = static_cast<device  __derived_t &>(*this);
    __metal_fence_texture_cube_t(derived.t);
  }
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC void fence() device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<device coherent(device) __derived_t &>(*this);
    __metal_fence_texture_cube_t(derived.t);
  }
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC void fence() constant 
  {
    const constant  auto &derived = static_cast<constant  __derived_t &>(*this);
    __metal_fence_texture_cube_t(derived.t);
  }
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC void fence() ray_data 
  {
    const ray_data  auto &derived = static_cast<ray_data  __derived_t &>(*this);
    __metal_fence_texture_cube_t(derived.t);
  }
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC void fence() object_data 
  {
    const object_data  auto &derived = static_cast<object_data  __derived_t &>(*this);
    __metal_fence_texture_cube_t(derived.t);
  }
#endif
#endif

  METAL_FUNC uint get_width(uint lod = 0) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_get_width_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_height(uint lod = 0) const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_get_height_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_num_mip_levels() const thread 
  {
    const thread  auto &derived = static_cast<const thread  __derived_t &>(*this);
    return __metal_get_num_mip_levels_texture_cube_t(derived.t);
  }
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC uint get_width(uint lod = 0) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_get_width_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_height(uint lod = 0) const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_get_height_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_num_mip_levels() const device 
  {
    const device  auto &derived = static_cast<const device  __derived_t &>(*this);
    return __metal_get_num_mip_levels_texture_cube_t(derived.t);
  }
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC uint get_width(uint lod = 0) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_get_width_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_height(uint lod = 0) const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_get_height_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_num_mip_levels() const device coherent(device)
  {
    const device coherent(device) auto &derived = static_cast<const device coherent(device) __derived_t &>(*this);
    return __metal_get_num_mip_levels_texture_cube_t(derived.t);
  }
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC uint get_width(uint lod = 0) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_get_width_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_height(uint lod = 0) const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_get_height_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_num_mip_levels() const constant 
  {
    const constant  auto &derived = static_cast<const constant  __derived_t &>(*this);
    return __metal_get_num_mip_levels_texture_cube_t(derived.t);
  }
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC uint get_width(uint lod = 0) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_get_width_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_height(uint lod = 0) const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_get_height_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_num_mip_levels() const ray_data 
  {
    const ray_data  auto &derived = static_cast<const ray_data  __derived_t &>(*this);
    return __metal_get_num_mip_levels_texture_cube_t(derived.t);
  }
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC uint get_width(uint lod = 0) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_get_width_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_height(uint lod = 0) const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_get_height_texture_cube_t(derived.t, lod);
  }
  METAL_FUNC uint get_num_mip_levels() const object_data 
  {
    const object_data  auto &derived = static_cast<const object_data  __derived_t &>(*this);
    return __metal_get_num_mip_levels_texture_cube_t(derived.t);
  }
#endif
};

#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
template <typename T, access a, memory_coherence c, typename _Enable>
struct texturecube : _texturecube<T, a, c>
#else
template <typename T, access a, typename _Enable>
struct texturecube : _texturecube<T, a, __memory_coherence_threadgroup>
#endif
{
  static_assert(is_color_texture_channel<T>::value, "invalid channel type 'T' for 'texturecube'");
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  static_assert(_is_valid_memory_coherence(c), "invalid memory_coherence");
#endif

  texturecube() thread = delete;
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  texturecube() constant = delete;
#endif
#if defined(__HAVE_RAYTRACING__)
  texturecube() ray_data = delete;
#endif

  METAL_FUNC texturecube(const thread  texturecube &) thread = default;
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const device  texturecube &) thread = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC texturecube(const device coherent(device) texturecube &) thread = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const constant  texturecube &) thread = default;
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const ray_data  texturecube &) thread = default;
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC texturecube(const object_data  texturecube &) thread = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const thread  texturecube &) constant = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const device  texturecube &) constant = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC texturecube(const device coherent(device) texturecube &) constant = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const constant  texturecube &) constant = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const ray_data  texturecube &) constant = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  METAL_FUNC texturecube(const object_data  texturecube &) constant = default;
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const thread  texturecube &) ray_data = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const device  texturecube &) ray_data = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const device coherent(device) texturecube &) ray_data = default;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const constant  texturecube &) ray_data = default;
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const ray_data  texturecube &) ray_data = default;
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const object_data  texturecube &) ray_data = default;
#endif
#endif

  thread  texturecube &operator=(const thread  texturecube &) thread  = delete;
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  thread  texturecube &operator=(const device  texturecube &) thread  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  thread  texturecube &operator=(const device coherent(device) texturecube &) thread  = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  thread  texturecube &operator=(const constant  texturecube &) thread  = delete;
#endif
#if defined(__HAVE_RAYTRACING__)
  thread  texturecube &operator=(const ray_data  texturecube &) thread  = delete;
#endif
#if defined(__HAVE_MESH__)
  thread  texturecube &operator=(const object_data  texturecube &) thread  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  device  texturecube &operator=(const thread  texturecube &) device  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  device  texturecube &operator=(const device  texturecube &) device  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  device  texturecube &operator=(const device coherent(device) texturecube &) device  = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  device  texturecube &operator=(const constant  texturecube &) device  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  device  texturecube &operator=(const ray_data  texturecube &) device  = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  device  texturecube &operator=(const object_data  texturecube &) device  = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  device coherent(device) texturecube &operator=(const thread  texturecube &) device coherent(device) = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  device coherent(device) texturecube &operator=(const device  texturecube &) device coherent(device) = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  device coherent(device) texturecube &operator=(const device coherent(device) texturecube &) device coherent(device) = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  device coherent(device) texturecube &operator=(const constant  texturecube &) device coherent(device) = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_RAYTRACING__)
  device coherent(device) texturecube &operator=(const ray_data  texturecube &) device coherent(device) = delete;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_MESH__)
  device coherent(device) texturecube &operator=(const object_data  texturecube &) device coherent(device) = delete;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_RAYTRACING__)
  ray_data  texturecube &operator=(const thread  texturecube &) ray_data  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  ray_data  texturecube &operator=(const device  texturecube &) ray_data  = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_RAYTRACING__)
  ray_data  texturecube &operator=(const device coherent(device) texturecube &) ray_data  = delete;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  ray_data  texturecube &operator=(const constant  texturecube &) ray_data  = delete;
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  ray_data  texturecube &operator=(const ray_data  texturecube &) ray_data  = delete;
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_RAYTRACING__)
  ray_data  texturecube &operator=(const object_data  texturecube &) ray_data  = delete;
#endif
#endif
#if defined(__HAVE_MESH__)
  object_data  texturecube &operator=(const thread  texturecube &) object_data  = delete;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  object_data  texturecube &operator=(const device  texturecube &) object_data  = delete;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_MESH__)
  object_data  texturecube &operator=(const device coherent(device) texturecube &) object_data  = delete;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  object_data  texturecube &operator=(const constant  texturecube &) object_data  = delete;
#endif
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_RAYTRACING__)
  object_data  texturecube &operator=(const ray_data  texturecube &) object_data  = delete;
#endif
#endif
#if defined(__HAVE_MESH__)
  object_data  texturecube &operator=(const object_data  texturecube &) object_data  = delete;
#endif

private:
  __metal_texture_cube_t t;

#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  friend METAL_FUNC bool metal::is_null_texture<T, a, c>(texturecube<T, a, c>);
  friend struct _texturecube_sample<T, a, c>;
  friend struct _texturecube_read<T, a, c>;
  friend struct _texturecube_write<T, a, c>;
  friend struct _texturecube_gather<T, a, c>;
  friend struct _texturecube<T, a, c>;
#if defined(__HAVE_TEXTURE_CUBE_RELAXED_ORDER_ATOMIC__)
  friend struct _texturecube_atomic_load<T, a, c>;
  friend struct _texturecube_atomic_store<T, a, c>;
  friend struct _texturecube_atomic_compare_exchange<T, a, c>;
  friend struct _texturecube_atomic_exchange<T, a, c>;
  friend struct _texturecube_atomic_fetch_and<T, a, c>;
  friend struct _texturecube_atomic_fetch_and_arith<T, a, c>;
  friend struct _texturecube_atomic_modify<T, a, c>;
#endif
#else
#if defined(__HAVE_NULL_TEXTURE__)
  friend METAL_FUNC bool metal::is_null_texture<T, a>(texturecube<T, a>);
#endif
  friend struct _texturecube_sample<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_read<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_write<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_gather<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube<T, a, __memory_coherence_threadgroup>;
#if defined(__HAVE_TEXTURE_CUBE_RELAXED_ORDER_ATOMIC__)
  friend struct _texturecube_atomic_load<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_store<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_compare_exchange<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_exchange<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_fetch_and<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_fetch_and_arith<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_modify<T, a, __memory_coherence_threadgroup>;
#endif
#endif
};

#if defined(__HAVE_DEFAULT_CONSTRUCTABLE_TEXTURE__) && defined(__HAVE_COPY_ASSIGNABLE_TEXTURE__)
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
template <typename T, access a, memory_coherence c>
struct texturecube<T, a, c, typename enable_if<_is_valid_default_construct(a, c) && _is_valid_copy_assign(a, c)>::type> : _texturecube<T, a, c>
#else
template <typename T, access a>
struct texturecube<T, a, typename enable_if<_is_valid_default_construct(a, __memory_coherence_threadgroup) && _is_valid_copy_assign(a, __memory_coherence_threadgroup)>::type> : _texturecube<T, a, __memory_coherence_threadgroup>
#endif
{
  static_assert(is_color_texture_channel<T>::value, "invalid channel type 'T' for 'texturecube'");
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  static_assert(_is_valid_memory_coherence(c), "invalid memory_coherence");
#endif

  METAL_FUNC texturecube() thread : t(__metal_get_null_texture_cube_t()) {}
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube() constant : t(__metal_get_null_texture_cube_t()) {}
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube() ray_data : t(__metal_get_null_texture_cube_t()) {}
#endif

  METAL_FUNC texturecube(const thread  texturecube &) thread = default;
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const device  texturecube &) thread = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC texturecube(const device coherent(device) texturecube &) thread = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const constant  texturecube &) thread = default;
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const ray_data  texturecube &) thread = default;
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC texturecube(const object_data  texturecube &) thread = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const thread  texturecube &) constant = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const device  texturecube &) constant = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC texturecube(const device coherent(device) texturecube &) constant = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC texturecube(const constant  texturecube &) constant = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const ray_data  texturecube &) constant = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  METAL_FUNC texturecube(const object_data  texturecube &) constant = default;
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const thread  texturecube &) ray_data = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const device  texturecube &) ray_data = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const device coherent(device) texturecube &) ray_data = default;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const constant  texturecube &) ray_data = default;
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const ray_data  texturecube &) ray_data = default;
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC texturecube(const object_data  texturecube &) ray_data = default;
#endif
#endif

  METAL_FUNC thread  texturecube &operator=(const thread  texturecube &) thread  = default;
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC thread  texturecube &operator=(const device  texturecube &) thread  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC thread  texturecube &operator=(const device coherent(device) texturecube &) thread  = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC thread  texturecube &operator=(const constant  texturecube &) thread  = default;
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC thread  texturecube &operator=(const ray_data  texturecube &) thread  = default;
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC thread  texturecube &operator=(const object_data  texturecube &) thread  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC device  texturecube &operator=(const thread  texturecube &) device  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC device  texturecube &operator=(const device  texturecube &) device  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC device  texturecube &operator=(const device coherent(device) texturecube &) device  = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
  METAL_FUNC device  texturecube &operator=(const constant  texturecube &) device  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC device  texturecube &operator=(const ray_data  texturecube &) device  = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  METAL_FUNC device  texturecube &operator=(const object_data  texturecube &) device  = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC device coherent(device) texturecube &operator=(const thread  texturecube &) device coherent(device) = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC device coherent(device) texturecube &operator=(const device  texturecube &) device coherent(device) = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC device coherent(device) texturecube &operator=(const device coherent(device) texturecube &) device coherent(device) = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
  METAL_FUNC device coherent(device) texturecube &operator=(const constant  texturecube &) device coherent(device) = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC device coherent(device) texturecube &operator=(const ray_data  texturecube &) device coherent(device) = default;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_MESH__)
  METAL_FUNC device coherent(device) texturecube &operator=(const object_data  texturecube &) device coherent(device) = default;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC ray_data  texturecube &operator=(const thread  texturecube &) ray_data  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC ray_data  texturecube &operator=(const device  texturecube &) ray_data  = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC ray_data  texturecube &operator=(const device coherent(device) texturecube &) ray_data  = default;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC ray_data  texturecube &operator=(const constant  texturecube &) ray_data  = default;
#endif
#endif
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC ray_data  texturecube &operator=(const ray_data  texturecube &) ray_data  = default;
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC ray_data  texturecube &operator=(const object_data  texturecube &) ray_data  = default;
#endif
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC object_data  texturecube &operator=(const thread  texturecube &) object_data  = default;
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  METAL_FUNC object_data  texturecube &operator=(const device  texturecube &) object_data  = default;
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_COHERENT__)
#if defined(__HAVE_MESH__)
  METAL_FUNC object_data  texturecube &operator=(const device coherent(device) texturecube &) object_data  = default;
#endif
#endif
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#endif
#if defined(__HAVE_INDIRECT_ARGUMENT_BUFFER__)
#if defined(__HAVE_MESH__)
  METAL_FUNC object_data  texturecube &operator=(const constant  texturecube &) object_data  = default;
#endif
#endif
#if defined(__HAVE_MESH__)
#if defined(__HAVE_RAYTRACING__)
  METAL_FUNC object_data  texturecube &operator=(const ray_data  texturecube &) object_data  = default;
#endif
#endif
#if defined(__HAVE_MESH__)
  METAL_FUNC object_data  texturecube &operator=(const object_data  texturecube &) object_data  = default;
#endif

private:
  __metal_texture_cube_t t;

  METAL_FUNC texturecube(__metal_texture_cube_t t) thread : t(t) {}

#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
  friend METAL_FUNC bool metal::is_null_texture<T, a, c>(texturecube<T, a, c>);
  friend struct _texturecube_sample<T, a, c>;
  friend struct _texturecube_read<T, a, c>;
  friend struct _texturecube_write<T, a, c>;
  friend struct _texturecube_gather<T, a, c>;
  friend struct _texturecube<T, a, c>;
#if defined(__HAVE_TEXTURE_CUBE_RELAXED_ORDER_ATOMIC__)
  friend struct _texturecube_atomic_load<T, a, c>;
  friend struct _texturecube_atomic_store<T, a, c>;
  friend struct _texturecube_atomic_compare_exchange<T, a, c>;
  friend struct _texturecube_atomic_exchange<T, a, c>;
  friend struct _texturecube_atomic_fetch_and<T, a, c>;
  friend struct _texturecube_atomic_fetch_and_arith<T, a, c>;
  friend struct _texturecube_atomic_modify<T, a, c>;
#endif
  template <bool Enable, typename From, typename To>
  friend struct _texture_explicit_conversion;
  template <bool Enable, typename From, typename To>
  friend struct _texture_implicit_conversion;
#else
#if defined(__HAVE_NULL_TEXTURE__)
  friend METAL_FUNC bool metal::is_null_texture<T, a>(texturecube<T, a>);
#endif
  friend struct _texturecube_sample<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_read<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_write<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_gather<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube<T, a, __memory_coherence_threadgroup>;
#if defined(__HAVE_TEXTURE_CUBE_RELAXED_ORDER_ATOMIC__)
  friend struct _texturecube_atomic_load<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_store<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_compare_exchange<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_exchange<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_fetch_and<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_fetch_and_arith<T, a, __memory_coherence_threadgroup>;
  friend struct _texturecube_atomic_modify<T, a, __memory_coherence_threadgroup>;
#endif
#endif
};
#endif

#if defined(__HAVE_NULL_TEXTURE__)
#if defined(__HAVE_DEVICE_COHERENT_READ_WRITE_TEXTURES__)
template <typename T, access a, memory_coherence c>
METAL_FUNC bool is_null_texture(texturecube<T, a, c> tex)
#else
template <typename T, access a>
METAL_FUNC bool is_null_texture(texturecube<T, a> tex)
#endif
{
  return __metal_is_null_texture_cube_t(tex.t);
}
#endif
} // namespace metal
#pragma METAL internals : disable

#endif // __BITS_METAL_TEXTURECUBE
